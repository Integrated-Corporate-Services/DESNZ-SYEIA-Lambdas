{
  "name": "govuk-notify-processor-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda for sending emails via GOV.UK Notify - processes SQS messages from backend",
  "type": "module",
  "main": "handler.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "deploy": "zip -r lambda.zip . -x '*.git*' 'node_modules/*' 'tests/*' && echo 'Upload lambda.zip to AWS Lambda'"
  },
  "keywords": [
    "govuk-notify",
    "aws-lambda",
    "email",
    "sqs"
  ],
  "author": "DESNZ Cloud Integration Team",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.943.0",
    "@aws-sdk/client-secrets-manager": "^3.943.0",
    "axios": "^1.7.0",
    "pg": "^8.16.3",
    "winston": "^3.18.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^30.2.0"
  }
}
